from __future__ import annotations

# This module is overwritten during the client build to embed the Supabase backend
# inside the executable (so the client cannot change it via local files).

SUPABASE_URL = ""
SUPABASE_KEY = ""

